---
description: 运营向产品设计 — 运营/管理端分离，禁止堆无用功能（做产品/UI 必读）
alwaysApply: true
---

# 运营向产品设计

产品界面分为 **运营** 与 **管理端** 两层，**不得混在同一首屏**。

- **运营**：日常干活的人（内容、投放、审核）
- **管理端**：管理员、项目负责人、开发排障与集成配置（含 Trace、Cron、Provider、环境变量等）

## 两类界面

| 类型 | 给谁看 | 放哪 | 原则 |
|------|--------|------|------|
| **运营** | 日常操作人员 | 工作台、队列、列表、向导 | 只回答：今天要做什么、点哪做、做完怎么知道 |
| **管理端** | 管理员、开发、排障 | 独立管理页、高级折叠、详情「高级信息」、`.env`/后台任务 | 与运营入口分离；嵌在运营页内时默认折叠 |

**禁止**把 Trace ID、Cron、Provider 名、SERP 调参、环境变量名等管理端信息放在运营首屏。

## 运营界面三问（新增功能前必答）

1. **对应流水线哪一步？**（配站点 → 选词 → 确认大纲 → 生成 → 改稿/审核 → 发布 → 看效果）
2. **是不是已有入口的重复？** 若是 → 加强现有入口，不新建菜单/卡片/筛选
3. **数字能否点一下去干活？** 不能 → 不要做成统计卡

## 禁止堆功能

- 概览**不要**成 BI 仪表盘：不可行动的指标（总数、排队中、优化中）合并或下沉
- **同一待办只出现一次**（待办列表 vs 统计卡 vs 导航角标 三选一为主，其余辅助）
- 顶栏菜单 **≤5 个一级**；低频能力（GSC、高级采集）放二级或站点内
- **SEO 工作台左侧导航**：概览 / 任务 / 词库 / 站点 + **设置**（仅 admin，含 CMS、页面库、GSC）
- 大纲待确认、敏感审核等队列**不进导航**，从概览流水线或任务列表 `?stage=` 进入
- 列表**默认列 ≤6**；技术分数、Trace、多维度筛选默认隐藏或合并为「阶段」
- 未启用/无数据的能力**不占位**（未连 GSC 不出 GSC 待办；无主题不显示主题卡）

## 文案

- 运营可见：**大纲**（非 Brief）、**重新生成**（非续跑）、**分析搜索结果**（非 SERP）
- 英文缩写仅出现在名词解释折叠区（YMYL、CMS、CTA 等）

## 交付前自检（运营向改动）

- [ ] 新能力是运营还是管理端？管理端是否独立入口或默认折叠？
- [ ] 是否减少而非增加运营首屏元素？
- [ ] 主路径是否 ≤3 次点击可达？
- [ ] 表单是否只暴露站点+关键词+意图，技术项在「高级选项」？
- [ ] 是否与现有流水线/待办/筛选重复？

## 反例 → 正例

| ❌ 反例 | ✅ 正例 |
|---------|---------|
| 概览 12 张统计卡 | 流水线 5 步 + 待办列表 |
| 任务列表 6 种筛选 + 6 条提示条 | 一个「阶段」筛选 + 站点 |
| 新建任务暴露 SERP 条数 | 高级选项折叠（管理端） |
| 每个队列都加站点筛选 | 多站点项目再在概览提升站点切换 |
| 为统计而加 API 字段 | 待办/流水线需要的计数才加 |
| 运营页直接展示 Trace / Cron | 管理端或「高级信息」折叠 |

## 参考流水线（seo-factory）

```
待确认大纲 → 生成中 → 待审核 → 待发布 → 需处理（失败/改稿）
```

运营向迭代优先：**减噪、合并入口、强化下一步按钮**；非必要不新增页面与筛选项。
