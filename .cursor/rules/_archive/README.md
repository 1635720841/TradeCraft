# 归档规则（只读）

本目录存放**历史/他项目** Cursor 规则，仅供对照查阅，**不参与**当前 wm 仓库的 active 规则加载。

- 勿将 `_archive` 内规则复制到上层 `.cursor/rules/` 除非明确要做考古对比
- 当前生效规则见 `.cursor/rules/00-core.mdc` 及同目录非 `_archive` 文件
- `merwise-dashboard-html/` 等已移除的旧资产不在本仓库维护
