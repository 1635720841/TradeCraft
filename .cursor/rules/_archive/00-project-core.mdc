---
description: 五大核心设计原则 + SaaS 企业平台愿景与技术栈
alwaysApply: true
---

# 五大核心设计原则（最高优先级）

**所有设计决策与代码实现，必须优先满足以下五点。发生冲突时，以此为准。**

| 原则 | 含义 | 落地要求 |
|------|------|---------|
| **拓展性** | 平台可纳新项目类型，企业可建多个项目 | 平台层与项目类型插件分离；新增类型 = 新插件注册，不改平台核心；MVP 可精简但**不破坏三层架构** |
| **解耦性** | 平台/插件/外部服务互不渗透 | 项目类型插件互不可见；业务 Service 禁止直接调外部 API；模块间通过**接口 + 事件**通信 |
| **低价平台可替代性** | 外部服务随时可换更便宜的供应商 | LLM/SERP/配图/Semrush 走**防腐层适配器**；切换供应商只改 `providers/`，业务零改动 |
| **模块化** | 平台层、项目插件、共享基建各司其职 | 平台管 auth/project/billing；`project-types/` 各管各的业务；`packages/` 放跨层共享 |
| **可维护性** | 新人可快速定位、排障、迭代 | TraceId 全链路；Prompt 外置；Schema 带注释；禁止 `any` 与空 catch |

## 平台定位

**一个 B2B SaaS 企业平台**，企业（Organization）注册后可在平台内**创建和管理多个项目**。

- **平台层**：统一登录、企业管理、项目 CRUD、计费、控制台
- **项目层**：企业内的独立工作空间，互不干扰
- **项目类型**：SEO 文章自动化（`seo-factory`）是第一个插件，后续可平行新增

## 当前首个项目类型：seo-factory

端到端：网站理解 → Serper SERP → LLM Brief/正文 → Semrush RPA → 内链 → Flux 配图 → HTML/JSON-LD 打包。

SEO 专属逻辑只存在于 `project-types/seo-factory/`，不得泄漏到平台层或其它项目类型。

## 辅助原则

- **双重隔离**：业务表含 `organizationId` + `projectId`，Service 层强制校验
- **低成本运营**：昂贵 API 必须 Redis 防御性缓存（24h 同指纹复用）
- **质量底线**：EEAT 信息增益，禁止洗稿；Semrush ≥9.5

## 架构师扩展维度（后期必备）

| 维度 | 规则文件 | 核心关注 |
|------|---------|---------|
| 安全 | `08-security` | 鉴权、IDOR 防越权、输入消毒、密钥、审计 |
| 性能 | `09-performance` | 索引、分页、缓存、队列并发、性能预算 |
| 可靠性 | `10-resilience` | 超时/重试/熔断、幂等、DLQ、优雅停机 |
| API 设计 | `11-api-design` | 版本、统一错误格式、DTO 分层、Webhook 验签 |
| 数据治理 | `12-data-governance` | 软删除、迁移策略、合规、事务一致性 |
| 可读性 | `13-code-readability` | 命名、复杂度上限、依赖方向、配置校验 |
| 测试运维 | `14-testing-ops` | 测试金字塔、Feature Flag、环境隔离、告警 |
| 后端工程 | `15-backend-engineering` | 文件头注释、模块结构、DI、DB、错误、日志 |
| 接口规范 | `16-api-standards` | 标准响应、版本化、异步 202、错误码、OpenAPI |
| 文件组织 | `17-file-organization` | 后端目录分层、命名、import 顺序 |
| 多语言 | `18-i18n` | vue-i18n、语言包、错误码映射、UI/内容语言分离 |
| 前端规范 | `19-frontend-standards` | Vue3 组件、Pinia、API 层、样式、性能 |
| 前端目录 | `20-frontend-file-organization` | 路由、组件、语言包、插件页面组织 |
| 工程化 | `21-tooling-templates` | ESLint、Prettier、Husky、BullMQ 模板 |

## 技术栈（禁止随意替换）

| 领域 | 选型 |
|------|------|
| 运行环境 | Node.js v20+ |
| 语言 | TypeScript (`strict: true`，禁用 `any`) |
| 前端 | Vue 3 + Vite + Pinia + vue-i18n |
| 后端 | NestJS（Controller-Service-Module，DDD） |
| 数据库 | PostgreSQL（重度 JSONB） |
| ORM | Prisma（Schema 须详细注释） |
| 队列/缓存 | Redis + BullMQ（>3s 任务必须入队） |
| RPA | Playwright + playwright-extra + stealth |
| 鉴权 | Logto / Supabase Auth |
| 日志 | winston 或 pino（必须支持 TraceId） |
