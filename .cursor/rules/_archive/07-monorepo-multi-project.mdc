---
description: SaaS 企业平台多项目管理 — 平台层 + 项目类型插件架构
alwaysApply: true
---

# SaaS 企业平台 · 多项目管理

**本系统是一个 B2B SaaS 企业平台**，企业（Organization）登录后可在平台内**创建和管理多个项目**。SEO 文章自动化只是第一个**项目类型（Project Type）**，未来可平行新增其它类型。

## 三层架构

```
┌─────────────────────────────────────────────────┐
│  平台层 Platform（统一入口，所有企业共用）          │
│  鉴权 · 企业/workspace · 项目管理 · 统一计费 · 控制台 │
└──────────────────────┬──────────────────────────┘
                       │ 1:N
┌──────────────────────▼──────────────────────────┐
│  项目层 Project（企业内的独立工作空间）             │
│  项目A: SEO工厂  │  项目B: SEO工厂  │  项目C: 未来类型 │
└──────────────────────┬──────────────────────────┘
                       │ 按 projectType 路由
┌──────────────────────▼──────────────────────────┐
│  项目类型插件 Project Type Plugin                 │
│  seo-factory（当前）│ [future-type]（预留）          │
└─────────────────────────────────────────────────┘
```

## 数据归属链

```
Organization（企业/租户）
  └── Project（项目，一个企业可建多个）
        ├── projectType: 'seo-factory' | 'future-xxx'
        ├── config: Json（项目级配置）
        └── 业务实体（按类型不同）
              seo-factory → Site, ArticleJob, ...
              future-xxx  → 该类型自己的表
```

**所有业务表必须同时含 `organizationId` + `projectId`，双重隔离。**

## 代码目录（Monorepo）

```
wm/
├── apps/platform/                  # 统一 SaaS 平台
│   ├── api/                        # NestJS：平台层 + 项目路由
│   │   └── src/
│   │       ├── modules/
│   │       │   ├── auth/           # 企业/用户/权限
│   │       │   ├── project/      # 项目 CRUD、类型注册、切换
│   │       │   └── billing/        # 统一计费（按项目归集）
│   │       └── project-types/      # 各项目类型插件（彼此隔离）
│   │           ├── seo-factory/  # 第一个项目类型
│   │           └── _template/    # 新项目类型脚手架
│   └── web/                        # 企业控制台（项目列表/切换/管理）
├── packages/
│   ├── shared-core/                # 日志、异常、TraceId、事件总线
│   ├── platform-sdk/               # 平台层接口（IProjectTypePlugin 等）
│   └── provider-interfaces/        # LLM/SERP 等防腐层接口
└── .cursor/rules/
```

## 项目类型插件铁律

| 规则 | 说明 |
|------|------|
| **插件互不可见** | `project-types/A` 禁止 import `project-types/B` |
| **只依赖平台接口** | 插件通过 `IProjectTypePlugin` 注册到平台，不直接耦合平台内部实现 |
| **统一入口** | 用户只访问一个控制台 URL，按当前选中 Project 路由到对应插件 |
| **计费归平台** | `CreditUsage` 在平台层记录，带 `organizationId` + `projectId` + `projectType` |
| **队列隔离** | Redis/BullMQ 用 `{projectType}:{projectId}:` 前缀 |

## 新增项目类型流程

1. 在 `project-types/` 下新建目录（如 `email-campaign/`）
2. 实现 `IProjectTypePlugin`（注册名、路由、菜单、API 前缀）
3. 在 `modules/project/` 的项目类型注册表中登记
4. 前端控制台自动出现新项目类型选项
5. **不改**平台层 auth/billing 核心逻辑

## 抽取到 packages/ 的时机

- 平台层 + 多个项目类型都需要 → `packages/`
- 仅某个项目类型专属 → 留在 `project-types/xxx/`
- 仅平台层用 → 留在 `modules/`
