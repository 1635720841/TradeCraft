---
description: 项目目录与文件组织规范 — 命名、分层、禁止事项
globs: "apps/**,packages/**"
alwaysApply: false
---

# 目录与文件组织规范

## 工作区顶层

```
wm/
├── apps/platform/
│   ├── api/                        # NestJS 后端
│   ├── web/                        # Vue3 前端
│   └── prisma/                     # 统一 Schema
├── packages/
│   ├── shared-core/                # 日志、异常、Context、工具
│   ├── shared-types/               # 跨包类型
│   └── platform-sdk/               # IProjectTypePlugin 等接口
├── docs/                           # 工作区文档
├── .cursor/rules/                  # Cursor 规则
├── pnpm-workspace.yaml
├── .env.example                    # 所有环境变量及说明
└── README.md
```

## 后端 api/src 分层

```
api/src/
├── main.ts                         # 启动入口
├── app.module.ts                   # 根模块
├── core/                           # 全局基建（与业务无关）
│   ├── config/                     # 环境变量校验 (zod)
│   ├── context/                    # RequestContext, CLS
│   ├── database/                   # PrismaService
│   ├── exceptions/                 # 异常类 + ExceptionFilter + error-codes.ts
│   ├── guards/                     # AuthGuard, ProjectGuard, RoleGuard
│   ├── interceptors/               # LoggingInterceptor, TransformInterceptor
│   ├── logger/                     # LoggerModule
│   ├── event-bus/                  # EventEmitter2 配置
│   └── decorators/                 # @Ctx(), @Roles(), @Idempotent()
├── modules/                        # 平台层业务
│   ├── auth/
│   ├── project/
│   └── billing/
└── project-types/                  # 项目类型插件
    └── seo-factory/
```

## 文件命名规则

| 类型 | 命名 | 示例 |
|------|------|------|
| 模块 | `{name}.module.ts` | `article-job.module.ts` |
| 控制器 | `{name}.controller.ts` | `article-job.controller.ts` |
| 服务 | `{name}.service.ts` | `article-job.service.ts` |
| DTO | `{action}-{name}.dto.ts` | `create-article-job.dto.ts` |
| 接口 | `{name}.interface.ts` | `serp-provider.interface.ts` |
| 适配器 | `{vendor}.adapter.ts` | `serper.adapter.ts` |
| 常量 | `{name}.constants.ts` | `job-status.constants.ts` |
| 测试 | `{name}.spec.ts` | `article-job.service.spec.ts` |
| 插件入口 | `{type}.plugin.ts` | `seo-factory.plugin.ts` |

- 全部 **kebab-case**；禁止 `ArticleJobService.ts`（PascalCase 文件名）
- 禁止 `utils.ts` / `helpers.ts` / `common.ts` 等笼统命名 — 用具体职责命名

## 前端目录

> 完整前端目录、路由、命名见 `20-frontend-file-organization.mdc`
> 多语言语言包见 `18-i18n.mdc`

## 禁止事项

| 禁止 | 原因 | 替代 |
|------|------|------|
| `index.ts` 桶文件 re-export 全部 | 循环依赖、tree-shaking 差 | 直接 import 具体文件 |
| 业务逻辑写在 Controller | 不可测试、不可复用 | 下沉 Service |
| 跨 project-type import | 插件耦合 | 走 platform-sdk 接口 |
| 根目录散落 `.ts` 文件 | 难定位 | 放入对应 module/core |
| `misc/` `temp/` `old/` 目录 | 垃圾堆 | 删除或用 Git 管理历史 |
| 一个文件多个 class | 超行数、职责混乱 | 一文件一主类 |

## Import 顺序

```typescript
// 1. Node 内置
import { randomUUID } from 'crypto';
// 2. 第三方
import { Injectable } from '@nestjs/common';
// 3. packages 共享
import { LoggerService } from '@packages/shared-core';
// 4. 本项目 core
import { RequestContext } from '@/core/context';
// 5. 同模块 / 邻模块
import { CreateArticleJobDto } from './dto/create-article-job.dto';
```

- 组间空一行；同组内字母序
- 路径别名统一 `@/` → `api/src/`
