---
description: AI 写代码防漂移铁律（全项目 AI 开发必读）
alwaysApply: true
---

# AI 防漂移铁律

> 详细守则：`.cursor/skills/seo-saas-platform/ai-guardrails.md`

## 写之前（强制）

1. 读 Skill + 对应 reference
2. **在仓库找同类文件当模板**，禁止从零发明结构
3. 过 `01-checklist.mdc`

## 写的时候（强制）

- **克隆不发明**：跟仓库已有写法，不引入新风格/新响应格式/新目录结构
- **范围锁**：只改用户要求的文件；禁止顺手重构、升级依赖、改无关文件
- **依赖锁**：禁止擅自 `npm install` 新包，先问用户
- **类型锁**：禁止 `any`、`@ts-ignore`、`as any` 放水
- **接线完整**：新 Module/API/页面必须注册（module、路由、error-codes），漏改 = 未完成

## 写之后（强制）

交付时列出：变更文件清单 + 接线确认 + 克隆自哪个模板

## 与现有代码冲突

- 用户没要求重构 → 新代码跟规范，旧代码不动
- 不凭「上次对话」记忆 → 以 rules/skills 和仓库代码为准
