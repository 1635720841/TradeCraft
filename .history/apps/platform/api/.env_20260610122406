# ── App ──
NODE_ENV=development
API_PORT=3000
WEB_PORT=5173
API_BASE_URL=http://localhost:3000

# ── Database ──
# 本地开发（Docker PostgreSQL：pnpm docker:up）
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wm_platform?schema=public

# ── Redis / BullMQ ──
REDIS_URL=redis://localhost:6379

# ── Auth (Logto / Supabase — 后期接入) ──
AUTH_ISSUER=
AUTH_AUDIENCE=

# ── Network proxy（Node fetch 默认不走系统代理，国内访问 Serper/DeepSeek 需配置）──
# Clash 默认端口 7890；无代理时注释掉下面三行
HTTPS_PROXY=http://127.0.0.1:7890
HTTP_PROXY=http://127.0.0.1:7890
NODE_USE_ENV_PROXY=1

# ── Providers (seo-factory) ──
PROVIDER_LLM=deepseek
PROVIDER_SERP=serper
PROVIDER_IMAGE=flux
DEEPSEEK_API_KEY=cmbUnionPay1780845070058dsHN
SERPER_API_KEY=sk-482d0e256abc40f4bcc4e5f76f6f1ba1
FLUX_API_KEY=

# ── Storage ──
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY=
S3_SECRET_KEY=
